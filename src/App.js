import { AppBar } from "@mui/material";
import { Container, Toolbar } from "@mui/material";

function App() {
  return (
    <AppBar>
      <Container fixed>
        <Toolbar></Toolbar>
      </Container>
    </AppBar>
  );
}

export default App;
